How to convert into .exe file


Open Terminal
pip install pyinstaller
pyinstaller *fileName.py* --onefile

dist folder is created in same filepath as the .py file, this holds the .exe file that should be generated.
